# Verbaia 开发约定

项目以根目录两份产品与 UI 规范为准：个人使用、仅英语、单体模块化架构。

- 前端使用 Next.js App Router，移动端优先；所有核心交互必须同时适配手机、平板与桌面。
- 后端使用 FastAPI 模块化单体；媒体导入、转写和 AI 生成一律走异步任务接口。
- 不引入社区、支付、租户、复杂角色、分布式服务或本地推理模型。
- 不提交密钥、真实用户数据、媒体文件、构建产物或本地数据库。
- 每一项可验收阶段完成后运行相应检查，并以中文 Git message 提交。
- 参考 FreeLingo 仅用于理解问题域；只有明确引入的上游文件才可复制，并须保留完整许可与署名。
